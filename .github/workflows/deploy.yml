name: Deploy agence-event to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via SFTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 148.230.127.222
          port: 65002
          protocol: sftp
          username: u234542620_wxTntinyR
          password: ${{ secrets.SFTP_PASSWORD }}
          local-dir: ./
          server-dir: /home/u234542620_wxTntinyR/public_html/
          exclude: |
            **/.git*
            **/.git*/**
            .github/**
            node_modules/**
            README.md
