name: Deploy agence-event to Hostinger

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          server: 148.230.127.222
          port: 65002
          username: u234542620_wxTntinyR
          password: ${{ secrets.SFTP_PASSWORD }}
          local_path: './*'
          remote_path: '/home/u234542620_wxTntinyR/public_html/'
          sftpArgs: '-o StrictHostKeyChecking=no'
          exclude: '.git*,.github'
